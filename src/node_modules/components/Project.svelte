<script>
    import { projectArticles, projectVersions } from 'stores/project';

    import Article from 'components/Article.svelte'

    export let basePath;
    export let metadata;
    export let monolingual;
    export let toc;
    export let content;
    export let versions = [];
    export let articles = [];

    $: $projectArticles = articles;
    $: $projectVersions = versions;
</script>

<Article {basePath} {metadata} {monolingual} {toc} {content}/>